<!DOCTYPE html>
<html lang="pt-br">
    <head>
        <%- include('partials/header') %>
        <link rel="shortcut icon" href="#" type="image/png">
        </head>
<body class="bg-gray-100 flex flex-col min-h-screen">
    
    <header class="bg-white shadow-md border-b-2 border-gray-100 sticky top-0 z-40">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex justify-between items-center h-20">
            <a href="/"><img src="https://upload.wikimedia.org/wikipedia/commons/8/8c/SENAI_S%C3%A3o_Paulo_logo.png" alt="Logo SENAI" class="h-10"></a>
            <div class="flex items-center space-x-6">
                <a href="/dashboard" class="text-gray-600 hover:text-red-700 font-semibold transition-colors duration-200 text-sm uppercase tracking-wider">
                    Dashboard
                </a>
                <a href="/perfil" class="text-gray-600 hover:text-red-700 font-semibold transition-colors duration-200 text-sm uppercase tracking-wider">
                    Meu Perfil
                </a>
                <span class="text-gray-300">|</span>
                <span class="text-gray-700 hidden sm:inline">Bem-vindo, <strong class="text-red-700"><%= professorName %></strong>!</span>
                <a href="/logout" class="inline-block px-4 py-2 rounded-md font-semibold text-center uppercase tracking-wider text-xs transition-transform duration-200 transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-offset-2 bg-red-700 text-white hover:bg-red-800 focus:ring-red-500">
                    Sair
                </a>
            </div>
        </div>
    </header>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 flex-grow w-full">
        <div class="space-y-8">
            <section class="bg-white rounded-lg shadow-lg p-6 border-t-4 border-red-700">
                <h2 class="text-xl font-bold text-gray-900 mb-6">Gestão de Turmas</h2>
                <div class="mb-4">
                    <button id="toggle-create-class-form" class="w-full text-left p-3 bg-gray-200 hover:bg-gray-300 rounded-md font-semibold text-gray-700 transition-colors">
                        + Adicionar Nova Turma
                    </button>
                </div>
                <div id="create-class-form-container" class="mb-6 p-4 bg-gray-50 rounded-lg border border-gray-200 hidden">
                    <div class="flex gap-3">
                        <input type="text" id="newClassName" placeholder="Nome da nova turma" class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-2 focus:ring-red-600 outline-none flex-1">
                        <button id="createClassBtn" class="inline-block px-6 py-2.5 rounded-md font-semibold text-center uppercase tracking-wider text-sm transition-transform duration-200 transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-offset-2 bg-red-700 text-white hover:bg-red-800 focus:ring-red-500 whitespace-nowrap">Criar Turma</button>
                    </div>
                </div>
                <div class="flex flex-col sm:flex-row gap-4 items-end">
                    <div class="flex-1 w-full">
                        <label for="classSelect" class="block text-sm font-medium text-gray-700 mb-2">Selecione uma Turma para Gerenciar:</label>
                        <select id="classSelect" class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-2 focus:ring-red-600 outline-none">
                            <option value="null">Selecione uma turma...</option>
                            <% if (classes.length > 0) { %>
                                <% classes.forEach(cls => { %>
                                    <option value="<%= cls.id %>"><%= cls.name %></option>
                                <% }) %>
                            <% } %>
                        </select>
                    </div>
                    <div class="flex gap-2">
                        <button id="shareClassBtn" class="inline-block px-6 py-2.5 rounded-md font-semibold text-center uppercase tracking-wider text-sm transition-transform duration-200 transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-offset-2 bg-blue-600 text-white hover:bg-blue-700 focus:ring-blue-500" disabled>Partilhar Turma</button>
                        <button id="editClassBtn" class="inline-block px-6 py-2.5 rounded-md font-semibold text-center uppercase tracking-wider text-sm transition-transform duration-200 transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-offset-2 bg-gray-600 text-white hover:bg-gray-700 focus:ring-gray-500" disabled>Editar Turma</button>
                        <button id="deleteClassBtn" class="inline-block px-6 py-2.5 rounded-md font-semibold text-center uppercase tracking-wider text-sm transition-transform duration-200 transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-offset-2 bg-red-700 text-white hover:bg-red-800 focus:ring-red-500" disabled>Remover Turma</button>
                    </div>
                </div>
            </section>
            
            <section id="student-management-panel" class="grid grid-cols-1 lg:grid-cols-2 gap-8">
                <div class="bg-white rounded-lg shadow-lg p-6 border-t-4 border-red-700">
                    <h3 class="text-lg font-semibold text-gray-800 mb-4">Meus Alunos</h3>
                    <div class="mb-4">
                        <input type="text" id="student-search-input" placeholder="Pesquisar em meus alunos..." class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-2 focus:ring-red-600 outline-none">
                    </div>
                    <div class="mb-4">
                        <button id="toggle-add-student-form" class="w-full text-left p-3 bg-red-100 hover:bg-red-200 rounded-md font-semibold text-red-800 transition-colors">
                            + Adicionar Novo Aluno
                        </button>
                    </div>
                    <div id="add-student-form-container" class="mb-4 p-4 bg-red-50 rounded-lg border border-red-200 hidden">
                        <form id="addStudentForm" class="space-y-2">
                           <input type="text" name="fullName" placeholder="Nome completo do aluno" class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-2 focus:ring-red-600 outline-none" required>
                           <input type="text" name="cpf" placeholder="CPF (opcional)" class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-2 focus:ring-red-600 outline-none">
                           <input type="text" name="pc_id" placeholder="ID do PC (opcional)" class="w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-2 focus:ring-red-600 outline-none">
                           <button type="submit" class="w-full inline-block px-6 py-2.5 rounded-md font-semibold text-center uppercase tracking-wider text-sm transition-transform duration-200 transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-offset-2 bg-red-700 text-white hover:bg-red-800 focus:ring-red-500">Adicionar</button>
                        </form>
                    </div>
                    <div id="all-students-list" class="space-y-2 max-h-96 overflow-y-auto"></div>
                </div>
                <div id="class-students-panel" class="bg-white rounded-lg shadow-lg p-6 border-t-4 border-red-700 hidden">
                    <h3 class="text-lg font-semibold text-gray-800 mb-4">Alunos na Turma: <span id="class-name-in-list" class="text-red-700"></span></h3>
                    <p class="text-sm text-gray-500 mb-4">Arraste um aluno da lista à esquerda para adicioná-lo aqui.</p>
                    <div id="students-in-class-list" class="space-y-2 min-h-[100px] bg-gray-50 p-2 rounded border-dashed border-2 border-gray-300"></div>
                </div>
            </section>
        </div>
    </main>

    <footer class="bg-red-800 text-white mt-auto">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6 text-center">
            <p class="text-sm">© <%= new Date().getFullYear() %> SENAI - V.O.C.E. Todos os direitos reservados.</p>
        </div>
    </footer>
    
    <%- include('partials/modals') %>
    
    <script src="/js/script.js"></script>
</body>
</html>